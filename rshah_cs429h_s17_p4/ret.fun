fun main() {
       print testlocalvars(50, 60, 70, 10, 20, 30, 50);       
}

fun testlocalvars(image, random, critter, tetris, boggle, treap, spider) {
    if ( 3 + 5__ <> (3 * stack(image, random, boggle))) { print 70;; }
    else {
         while (1) {
               print infiniteloop;
         }
         param = 8;
    }
    return (3 + image * (random + 10 == critter) <> critter) + boggle * spider;
}

fun stack(){
return 5;
}